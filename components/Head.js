import React from 'react';
const Head = (props) => {
  return(
      // Using Props handleClick as callback function
      <div>

        <p>Mustafa {props.id} </p>
      </div>
  );
}
export default Head;
